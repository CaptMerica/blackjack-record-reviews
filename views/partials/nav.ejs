  <% if (!user) { %>
    <script defer src="https://accounts.google.com/gsi/client"></script>
  <% } %>
</head>
<body>
  <nav>
    <i class="fas fa-solid fa-cat"> Blackjack Record Reviews</i>
    <i class="fas fa-solid fa-record-vinyl"></i>
    <a 
    href="/albums/new" id="add-review"<%- title === 'New Review' ? 'class="active"': "" %> 
    >
    Add Review
  </a>

    <a 
    href="/albums" id="all-albums"<%- title === "All Albums" ? 'class="active"' : ""  %> 
    >
    All Albums
    </a>
    <% if (user) { %>
      <a href="/auth/logout" class="right">
        <button type="submit">Logout</button>
      </a>
    <% } else { %>
      <div 
        id="g_id_onload"
        data-client_id="<%= googleClientID %>"
        data-login_uri="/auth/google"
        data-auto_prompt="false"
      ></div>
      <div 
        class="g_id_signin right"
        data-type="standard"
        data-size="large"
        data-theme="outline"
        data-text="sign_in_with"
        data-shape="rectangular"
        data-logo_alignment="left"
      ></div>
    <% } %>
  </nav>
